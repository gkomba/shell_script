#include <stdlib.h>

int	main(void)
{
	system("echo WELCOME GKOMBA");
	system("echo HAVE A NICE WORK!!!");
	system("echo UPDATING REPOSITORIES...");
	system("sudo apt update && upgrade");
	//system("code .");
	//system("google-chrome");
	//system("clear");
	return (0);
}
